import type { MarkdownOptions } from '@vuepress/markdown';
import type { HeadConfig, SiteLocaleConfig } from '@vuepress/shared';
import type { ThemeConfig } from '../theme';
/**
 * Vuepress app options
 */
export interface AppOptions<T extends ThemeConfig = ThemeConfig> {
    base: string;
    lang: string;
    title: string;
    description: string;
    head: HeadConfig[];
    locales: SiteLocaleConfig;
    theme: string;
    themeConfig: Partial<T>;
    source: string;
    dest: string;
    temp: string;
    cache: string;
    public: string;
    debug: boolean;
    host: string;
    port: number;
    open: boolean;
    evergreen: boolean;
    templateDev: string;
    templateSSR: string;
    shouldPreload: ((file: string, type: string) => boolean) | boolean;
    shouldPrefetch: ((file: string, type: string) => boolean) | boolean;
    markdown: MarkdownOptions;
    pagePatterns: string[];
}
export declare type AppConfig = Partial<AppOptions> & Pick<AppOptions, 'source'>;
//# sourceMappingURL=options.d.ts.map