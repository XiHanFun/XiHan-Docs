"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePagePath = void 0;
/**
 * Resolve the final path of a page
 */
const resolvePagePath = ({ permalink, pathInferred, options, }) => {
    var _a;
    // use permalink first
    if (permalink) {
        return permalink;
    }
    // use inferred path
    if (pathInferred) {
        // TODO: handle index file path
        return pathInferred.replace(/\/(README|index).html$/i, '/');
    }
    // use options path
    return (_a = options.path) !== null && _a !== void 0 ? _a : '';
};
exports.resolvePagePath = resolvePagePath;
//# sourceMappingURL=resolvePagePath.js.map