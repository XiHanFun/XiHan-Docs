import type { PageOptions, PageFrontmatter } from '../types';
/**
 * Resolve page permalink from frontmatter / options / pattern
 */
export declare const resolvePagePermalink: ({ options, frontmatter, slug, date, pathInferred, pathLocale, }: {
    options: PageOptions;
    frontmatter: PageFrontmatter;
    slug: string;
    date: string;
    pathInferred: string | null;
    pathLocale: string;
}) => string | null;
//# sourceMappingURL=resolvePagePermalink.d.ts.map