import { inject } from 'vue';
export const themeDataSymbol = Symbol(__DEV__ ? 'themeData' : '');
export const themeLocaleDataSymbol = Symbol(__DEV__ ? 'themeLocaleData' : '');
export const useThemeData = () => {
    const themeData = inject(themeDataSymbol);
    if (!themeData) {
        throw new Error('useThemeData() is called without provider.');
    }
    return themeData;
};
export const useThemeLocaleData = () => {
    const themeLocaleData = inject(themeLocaleDataSymbol);
    if (!themeLocaleData) {
        throw new Error('useThemeLocaleData() is called without provider.');
    }
    return themeLocaleData;
};
export const resolveThemeData = (siteData) => siteData.themeConfig;
export const resolveThemeLocaleData = resolveThemeData;
//# sourceMappingURL=themeData.js.map