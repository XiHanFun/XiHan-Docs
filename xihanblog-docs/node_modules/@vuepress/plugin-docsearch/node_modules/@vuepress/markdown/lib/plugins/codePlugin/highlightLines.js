"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isInHighlightLinesRanges = exports.resolveHighlightLinesRanges = void 0;
/**
 * Resolve highlight-lines ranges from the token info
 */
const resolveHighlightLinesRanges = (info) => {
    // try to match highlight-lines mark
    const match = info.match(/{([\d,-]+)}/);
    // no highlight-lines mark, return `null`
    if (match === null) {
        return null;
    }
    // resolve lines ranges from the highlight-lines mark
    return match[1].split(',').map((item) => {
        const range = item.split('-');
        if (range.length === 1) {
            range.push(range[0]);
        }
        return range.map((str) => Number.parseInt(str, 10));
    });
};
exports.resolveHighlightLinesRanges = resolveHighlightLinesRanges;
/**
 * Check if a line number is in ranges
 */
const isInHighlightLinesRanges = (lineNumber, ranges) => ranges.some(([start, end]) => lineNumber >= start && lineNumber <= end);
exports.isInHighlightLinesRanges = isInHighlightLinesRanges;
//# sourceMappingURL=highlightLines.js.map