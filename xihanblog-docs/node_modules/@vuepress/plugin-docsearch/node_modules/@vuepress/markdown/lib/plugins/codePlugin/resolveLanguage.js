"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveLanguage = void 0;
const languages = require("./languages");
/**
 * A key-value map to get language info from alias
 *
 * - key: alias
 * - value: language
 */
const languagesMap = Object.values(languages).reduce((result, item) => {
    item.aliases.forEach((alias) => {
        result[alias] = item;
    });
    return result;
}, {});
/**
 * Resolve language for highlight
 */
const resolveLanguage = (alias) => { var _a; return (_a = 
// if the alias does not have a match in the map
// fallback to the alias itself
languagesMap[alias]) !== null && _a !== void 0 ? _a : {
    name: alias,
    ext: alias,
    aliases: [],
}; };
exports.resolveLanguage = resolveLanguage;
//# sourceMappingURL=resolveLanguage.js.map